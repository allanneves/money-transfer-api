# Routes
# This file defines all application routes (Higher priority routes first)

# Perform money transfer
POST     /rest/v1/transfers                           transfers.controller.TransferController.transfer

GET      /rest/v1/accounts/:accountNumber/balance     accounts.controller.AccountController.checkBalance(accountNumber)
